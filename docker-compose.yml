
services:
  web:
    build: .
    container_name: nykaa_app
    command: gunicorn nykaa_backend.wsgi:application --bind 0.0.0.0:8000 --timeout 120
    volumes:
      - ./nykaa_backend:/app
      - ./nykaa_backend/media:/app/media
      - ./db.sqlite3:/app/db.sqlite3
    ports:
      - "8000:8000"
    env_file:
      - .env

